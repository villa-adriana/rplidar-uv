[project]
name = "rplidar-uv"
version = "0.1.0"
description = "Minimal RPLIDAR A1M8 example using uv"
requires-python = ">=3.10"
dependencies = [
    "rplidar>=0.9.2",
    "rich"
]

[project.scripts]
rplidar-uv = "plot:main"

